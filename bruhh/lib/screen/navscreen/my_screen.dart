import 'package:flutter/material.dart';

class MyScreenScreen extends StatelessWidget {
  const MyScreenScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("my page"),
    );
  }
}
